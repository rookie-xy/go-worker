log:
    name: simple
    level: info
    path: /data/logs/error.log

location:
    html: /data/service/html
    timeout: 10

inputs:
    - stdin:
          status: on
          channal: test

channals:
    - memory:
          name: test
          size: 2048

outputs:
    - stdout:
          status: on
          channal: test
